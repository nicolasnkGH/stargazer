{
  "version": "3.2.0",
  "description": "A personal, distraction-free stargazing dashboard and astronomy portal",
  "scripts": {
    "dev": "cd nextjs-app && npm run dev",
    "dev:all": "(cd api && ./venv/bin/uvicorn main:app --host 0.0.0.0 --port 8181 &) && cd nextjs-app && API_BACKEND=http://localhost:8181 npm run dev",
    "api": "cd api && ./venv/bin/uvicorn main:app --host 0.0.0.0 --port 8181 --reload",
    "build": "cd nextjs-app && npm run build",
    "lint": "cd nextjs-app && npm run lint",
    "test": "npx playwright test tests/smoke.spec.js"
  },
  "devDependencies": {
    "@lhci/cli": "^0.15.1",
    "@playwright/test": "^1.54.0"
  }
}