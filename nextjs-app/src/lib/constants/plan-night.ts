export const PLAN_NIGHT_STORAGE_KEY = "sg_night_plan";

/** Fallback dusk/dawn hours for the timeline bar when real seeing data isn't threaded in. */
export const DEFAULT_DUSK_HOUR = 18;
export const DEFAULT_DAWN_HOUR = 6;
